Changes in Dicer 0.2.1
----------------------

    * New options to the `dicer-cli policy` command:
      * `--show-owlapi-error`;
      * `--assume-manchester`.


Changes in Dicer 0.2.0
----------------------

    * Library:
      * Add ID generation facilities.
    * Command-line tool:
      * Move the existing features to a `policy` subcommand.
      * Add the `tsv` subcommand.
